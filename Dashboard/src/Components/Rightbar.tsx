import { Box } from "@mui/material"

const Rightbar = () => {
    return (
        <Box sx={{
            background: "green",
        }} flex={1} p={2}>
            Right
        </Box>
    )
}

export default Rightbar
