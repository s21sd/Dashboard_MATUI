import React from 'react'
import { Box } from "@mui/material"

const Navbar = () => {
    return (
        <Box sx={{
            background: "red",
        }} flex={2} p={2}>
            Navbar
        </Box>
    )
}

export default Navbar