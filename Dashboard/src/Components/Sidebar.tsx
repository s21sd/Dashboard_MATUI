import React from 'react'
import { Box } from "@mui/material"

const Sidebar = () => {
    return (
        <Box sx={{
            background: "blue",
        }} flex={1} p={2}>
            Sidebar
        </Box>
    )
}

export default Sidebar